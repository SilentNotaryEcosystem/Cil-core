syntax = "proto3";

package witness;

//TODO: replace  bytes content to oneof
message WitnessMessage {
    string groupName=1;
    bytes content=2;
}

// -------------- NEXT_ROUND -----
message NextRound {
    uint32 roundNo =1;
}
